<%= form_with(model: @word, id: "word_creator") do |f| %>
    <%= f.text_field :main_word, placeholder: true %>
    <%= f.submit %>
<% end %>

<ul id="words">
    <!-- Call a partial _secondary_language -->
    <%#= render @words %>
</ul>